<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:title="Giỏ hàng"
            android:id="@+id/gioHang"
            android:icon="@drawable/giohang"/>
        <item android:title="Đơn hàng"
            android:id="@+id/donHang"
            android:icon="@drawable/donhang"/>
        <item android:id="@+id/doiMatKhau"
              android:icon="@drawable/ic_loop_black_24dp"
              android:title="Đổi mật khẩu"
              android:iconTint="@android:color/white"/>
        <item android:id="@+id/dangXuat"
              android:icon="@drawable/ic_exit_to_app_black_24dp"
              android:title="Đăng xuất"
              android:iconTint="@android:color/white"/>
    </group>
</menu>